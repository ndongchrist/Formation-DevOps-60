apiVersion: v1
kind: Service
metadata:
  name: demo-service
spec:
  type: NodePort
  selector:
    app: demo-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30009

# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: prometheus-service
#   namespace: monitoring
# spec:
#   type: NodePort
#   selector:
#     app: prometheus
#   ports:
#     - port: 8080
#       targetPort: 8080
#       nodePort: 30090