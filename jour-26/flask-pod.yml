apiVersion: v1
kind: Pod
metadata:
  name: flask-pod
  labels:
    app: flask
spec:
  containers:
  - name: flask-image
    image: flask-image:latest
    ports:
    - containerPort: 5000
    imagePullPolicy: Never